# 配置示例文件
# 复制此文件为 config.yaml 并根据需要修改
#
# 此文件与 internal/config/config.go 中的 DefaultConfig() 保持同步
# 所有配置项都可以通过环境变量覆盖 (默认的环境变量前缀：APP_)
# 例如：APP_HOST=0.0.0.0 会覆盖 host 的值

addr: ":8080" # 服务器监听地址
dist_docs: "docs/.vitepress/dist" # VitePress 文档目录路径
